name: jksecurityapp
description: "JK Security Supervisor App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^14.0.3
  image_picker: ^1.2.3
  cupertino_icons: ^1.0.6
  http: ^1.6.0
  flutter_map: ^8.3.2
  latlong2: ^0.10.1
  socket_io_client: ^3.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true